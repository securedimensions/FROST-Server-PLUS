#based on the FROST-Server v2.5.3 for STAplus 1.0.2
services:
  web:
    build:
      context: ..
      dockerfile: Dockerfile.HTTP
    image: securedimensions/frost-server-v2.5.8_staplus:1.0.2
    environment:
      # For all settings see: https://fraunhoferiosb.github.io/FROST-Server/settings/settings.html
      - serviceRootUrl=http://localhost:8080/FROST-Server
      - plugins_multiDatastream.enable=true
      - http_cors_enable=true
      - http_cors_allowed_origins=*
      - persistence_db_driver=org.postgresql.Driver
      - persistence_db_url=jdbc:postgresql://database:5432/sensorthings
      - persistence_db_username=sensorthings
      - persistence_db_password=ChangeMe
      - persistence_autoUpdateDatabase=true
      - plugins_plugins=de.securedimensions.frostserver.plugin.staplus.PluginPLUS
      - plugins_staplus_enable=true
      - plugins_staplus_enable_enforceOwnership=true
      - plugins_staplus_enable_enforceLicensing=true
    ports:
      - 8080:8080
    depends_on:
      database:
        condition: service_healthy

  database:
    image: postgis/postgis:14-3.2-alpine
    environment:
      - POSTGRES_DB=sensorthings
      - POSTGRES_USER=sensorthings
      - POSTGRES_PASSWORD=ChangeMe
    volumes:
      - postgis_volume:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -d sensorthings -U sensorthings "]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
    postgis_volume:
